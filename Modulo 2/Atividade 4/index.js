function inverterString(str) {
    return str.split('').reverse().join('');
  }
  
  // Exemplo de uso:
  const texto = 'Olá, mundo!';
  const textoInvertido = inverterString(texto);
  console.log(textoInvertido); // "!odnum ,álO"
  